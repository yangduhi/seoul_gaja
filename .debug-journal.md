# Todo 10 RSC runtime debug journal

## Bound environment

- Worktree: `D:\vscode\seoul_gaja-worktrees\todo10-rsc-runtime-fix`
- Branch: `codex/todo10-rsc-runtime-fix`
- Exact parent: `323717c167111eea5f652ccaea7baf4c31a8a6e0`
- Exact parent tree: `4b9845e0133962620f7ad78701b62292a86a3902`
- Runtime: Node.js / Vinext / Vite RSC, exercised with cached Playwright and installed Chrome
- Evidence directory: `.omo/evidence/seoul-gaja-v4-plan-review/task-10-rsc-runtime-fix/`

## Hypotheses and distinguishing checks

1. **The one-shot `__VINEXT_RSC_NAVIGATE__` wrapper is consumed before the catalog-root replay.** Distinguishing check: retain every `requestfailed` URL and correlate it with the close and Back state snapshots; inspect whether the request appears only after the later Back even though the guard was armed during close.
2. **Vinext's popstate handler closes over its navigation function, so replacing the window global cannot affect the active handler.** Distinguishing check: inspect the installed Vinext browser runtime for handler registration and whether it calls a captured `navigate` binding or reads `window.__VINEXT_RSC_NAVIGATE__` at event time.
3. **Any same-document `popstate` observed by Vinext starts an RSC refresh even when pathname/search are unchanged.** Distinguishing check: use the real browser matrix to compare close-only `replaceState` (no event) with the subsequent same-URL browser Back (`popstate`); the latter should be the exact trigger for `/.rsc?...` aborts.
4. **A navigation-free in-app replay can preserve the two-entry contract without presenting a popstate to Vinext.** Distinguishing check: add a failing source seam for an idempotent history listener installed before the router listener, then rerun the real browser journeys and require `failedCount=0` and `rscFailureCount=0` without filtering telemetry.

## Artifact ledger

| Artifact | Purpose | Cleanup disposition |
| --- | --- | --- |
| `node_modules` junction | Reuse exact parent dependencies | Remove before closeout |
| QA server process and isolated port | Real Vinext reproduction | Terminate process tree and prove port free |
| Installed Chrome contexts | Real browser reproduction | Close contexts/browser before closeout |
| `.omo/evidence/seoul-gaja-v4-plan-review/task-10-rsc-runtime-fix/*` | RED/GREEN and verification evidence | Retain and commit |

## Observations

- Exact-parent focused baseline: 25/25 PASS, proving the existing source seam did not cover the browser lifecycle defect.
- Exact-parent real-browser RED: 6 `requestfailed` events, all `fetch` requests to `/.rsc?visualFixture=ready-v1&_rsc` with `net::ERR_ABORTED`; zero console/page errors. See `baseline/browser-network-diagnostic.json`.
- The first copied harness attempt omitted `visualFixture=ready-v1` from the invalid-route request and timed out before network aggregation. The harness was corrected to exercise the official invalid fixture; this setup error is not used as RED evidence.
- Runtime inspection proved Vinext reads `window.__VINEXT_RSC_NAVIGATE__` at popstate event time, so hypothesis 2 was rejected.
- Close followed by Back passed because the old one-shot guard was armed by close. Direct browser Back from the sheet reproduced the RSC abort because no close had armed it, confirming hypotheses 1 and 3.
- The failing seam required a catalog-lifetime traversal guard and failed 1/11 on the unchanged parent. See `baseline/zero-rsc-seam-red.log`.
- Minimal fix: install one reversible wrapper while `CatalogSurface` is mounted; suppress only Vinext `navigationKind === "traverse"` when the destination is the `/` `catalog-root` sentinel. All other navigations delegate to the original runtime function.
- Exact implementation candidate `802a27e979c5698c53543aa3b1dd2394a2dbcc18` / tree `036711dda15d6edf61fe47ee8d8294712ae7456d` passed the two-pass four-viewport matrix with zero failed/RSC/console events.
