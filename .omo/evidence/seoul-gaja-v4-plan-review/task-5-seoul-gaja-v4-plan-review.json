{
  "schema_version": 2,
  "task": "Todo 5 - candidate-bound Phase 02 automation gate",
  "observed_at": "2026-08-06T00:00:00+09:00",
  "verdict": "NOT_RUN_BLOCKED",
  "binding": {
    "base_commit": "390e32d3c237214696dcee2cb8231f686e1b4628",
    "implementation_tree_before_evidence": "8aa3f0cc441fdc43976dff9ccba5bc08eda2bbff",
    "plan_sha256": "4a15e30af972c77055076b6f75a3e72326e76725d5ccb0d982ef8beddedf3850",
    "note": "The final commit hash is reported by the atomic Git handoff rather than embedded in this same commit, which would create a self-referential hash."
  },
  "baseline_red": {
    "command": "node --test tests/gates/task-05-failure.test.mjs",
    "actual_exit": 1,
    "observed_failure": "Missing candidate tree and plan bindings incorrectly returned PASS."
  },
  "branch_local": {
    "verdict": "PASS",
    "checks": [
      "candidate SHA, tree, and plan SHA-256 equal their reviewed bindings",
      "each successful local test carries the same candidate SHA, tree, and plan SHA-256",
      "failure fixtures return named NOT_RUN_BLOCKED rather than PASS"
    ]
  },
  "default_branch_smoke": {
    "verdict": "NOT_RUN_BLOCKED",
    "blocker": "No owner-approved merge to main, default-branch workflow run, or non-production endpoint proof is authorized in this local task.",
    "owner_action": "Approve and complete the exact candidate merge to main, then authorize a default-branch workflow smoke against a non-production endpoint and provide its run proof and candidate-bound receipt.",
    "external_invocation": "NONE"
  },
  "commands": [
    { "id": "task-05-happy", "actual_exit": 0, "verdict": "PASS" },
    { "id": "task-05-failure", "actual_exit": 0, "verdict": "PASS" },
    { "command": "node --test tests/gates/task-05-happy.test.mjs tests/gates/task-05-failure.test.mjs", "actual_exit": 0, "result": "PASS: 8/8" },
    { "command": "repeat focused Node tests", "actual_exit": 0, "result": "PASS: 8/8" },
    { "command": "python docs/execution/scripts/validate_authority_lock.py", "actual_exit": 0, "result": "PASS" },
    { "command": "python docs/execution/scripts/validate_command_map.py docs/execution/contracts/execution-command-map.json", "actual_exit": 0, "result": "PASS: 30 entries" },
    { "command": "git diff --check", "actual_exit": 0, "result": "PASS" }
  ],
  "ultraqa": {
    "malformed_input": "PASS: malformed-record.json returned MALFORMED_RECORD with exit 3",
    "prompt_injection": "PASS: untrusted workflow_text could not override a stale workflow head SHA",
    "cancel_resume": "NOT_APPLICABLE: pure one-shot evaluator",
    "stale_state": "PASS: stale main and mismatched receipt returned NOT_RUN_BLOCKED",
    "dirty_worktree": "PASS: initial target worktree was clean and no unrelated paths were modified",
    "hung_or_long_commands": "PASS: all focused commands completed within the local command timeout",
    "flaky_tests": "PASS: focused suite repeated with 8/8 passing each time",
    "misleading_success_output": "PASS: verdicts depend on parsed SHA/tree/receipt fields, not wrapper text",
    "repeated_interruptions": "PASS: no process, port, server, workflow, or temporary resource was created"
  },
  "cleanup": {
    "workflow_runs": 0,
    "server_or_browser": 0,
    "ports": 0,
    "temporary_directories": 0,
    "secrets": 0,
    "push": 0,
    "deploy": 0,
    "merge": 0
  }
}
