# Design Pack

`index.html`을 브라우저에서 열면 전체 concept board와 mockup을 볼 수 있다.

## 구현 권위

1. `design.md`
2. `design-tokens.json`
3. `component-contracts.md`
4. `screen-specs.md`
5. `mockups/*.png`

## 영감용

`ai-concept-boards/`의 이미지는 시각 방향 탐색용이다. 생성 과정에서 문구·숫자·아이콘이 부정확할 수 있으므로 구현 계약으로 사용하지 않는다.
